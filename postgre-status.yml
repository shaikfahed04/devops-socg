---
hosts: all
become: yes
become_user: postgres


tasks:
  - name: Check the postgres db status
    shell: pg_ctl status
    register: status

tasks:
  - name: print the status
    debug:
      var: status
