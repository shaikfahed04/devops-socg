---
hosts: all
become_user: yes
become: postgres


tasks:
  - name: Check the postgres db status
    shell: pg_ctl status
    register: status

tasks:
  - name: print the status
    debug:
      var: status
