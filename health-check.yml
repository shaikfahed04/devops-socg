host: all
become_user: yes
become: postgres


- task:
    name: Check the postgres db status
    shell: pg_ctl status
    register: status

- task:
    name: print the status
    debug:
      var: status
